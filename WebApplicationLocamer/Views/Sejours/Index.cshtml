@model IEnumerable<WebApplicationLocamer.Models.Sejour>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_option)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_debut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_fin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.nom_client)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type_sejour.libelle_Tsejour)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_option)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_debut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_fin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.nom_client)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type_sejour.libelle_Tsejour)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Sejour }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Sejour }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Sejour })
        </td>
    </tr>
}

</table>
