@model IEnumerable<WebApplicationLocamer.Models.Type_tarif>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelle_Ttarif)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prixJour_Ttarif)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelle_Ttarif)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prixJour_Ttarif)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Ttarif }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Ttarif }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Ttarif })
        </td>
    </tr>
}

</table>
